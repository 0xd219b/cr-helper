//! Input handling

/// Input handler
pub struct InputHandler;

impl InputHandler {
    /// Create a new input handler
    pub fn new() -> Self {
        Self
    }
}

impl Default for InputHandler {
    fn default() -> Self {
        Self::new()
    }
}
